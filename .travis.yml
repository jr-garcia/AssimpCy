language: python
sudo: false

addons:
  apt:
    packages:
      - libassimp
      - libassimp-dev

before_install:
- pip install -r ./requirements.txt

install:
  - python setup.py build
  - python setup.py install